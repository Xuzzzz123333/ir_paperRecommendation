# Frontend

## 安装依赖

```bash
cd frontend
npm install
```

## 启动开发服务器

```bash
npm run dev
```

默认连接的后端地址为 `http://localhost:8000`。

如果需要修改后端地址，可以在启动前设置：

```bash
VITE_API_BASE_URL=http://localhost:8000 npm run dev
```
